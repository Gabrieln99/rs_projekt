# Decentralized Lost & Found

Checkpoint 1 monolithic FastAPI application with in-memory storage and simulated blockchain behavior.

## Run

```bash
uvicorn main:app --reload
```

Open `http://127.0.0.1:8000/docs` to test the API in Swagger UI.